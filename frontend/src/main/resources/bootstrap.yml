spring:
  application:
    name: frontend
  cloud:
    config:
#      uri: http://config:8888
      uri: http://localhost:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
